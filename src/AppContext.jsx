import { createContext } from "react";

export const AppContext = createContext();

export const Welcome = createContext();

export const Clicky = createContext();

export const Mobile = createContext();

export const User = createContext();

export const Navmode = createContext();

export const UserOptions = createContext();

export const UserL = createContext();
